name: 'Check Rendering'
on:
  pull_request:
    types: [opened, synchronize]
jobs:
  check-render:
    runs-on: 'ubuntu-latest'
    steps:
    - uses: awalsh128/cache-apt-pkgs-action@latest
      with:
        packages: graphviz
        version: 1.0
    - uses: taiki-e/cache-cargo-install-action@latest
      with:
        tool: mdbook
    - uses: taiki-e/cache-cargo-install-action@latest
      with:
        tool: mdbook-graphviz
    - uses: actions/checkout@v3
    - run: mdbook build


