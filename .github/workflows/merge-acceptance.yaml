name: 'Check Rendering'
on:
  pull_request:
    types: [opened, synchronize]
jobs:
  check-render:
    runs-on: 'ubuntu-latest'
    steps:
    - uses: awalsh128/cache-apt-pkgs-action@v1
      with:
        packages: graphviz
        version: 1.0
    - uses: taiki-e/cache-cargo-install-action@v1
      with:
        tool: mdbook
    - uses: taiki-e/cache-cargo-install-action@v1
      with:
        tool: mdbook-graphviz
    - uses: actions/checkout@v3
    - run: mdbook build


